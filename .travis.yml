language: node_js
node_js:
  - 6
cache:
  directories:
    - node_modules
    - app/node_modules
script:
  - npm run flow
  - npm test
  - npm build
  - npm install --prefix app
  - npm test --prefix app
  - npm run build --prefix app