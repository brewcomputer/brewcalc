language: node_js
node_js:
  - 6
cache:
  directories:
    - node_modules
script:
  - npm run flow
  - npm test
  - npm build
  - npm install --prefix app
  - npm run flow --prefix app
  - npm test --prefix app