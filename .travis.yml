language: node_js
node_js:
  - 6

before_install:
  npm install -g npm@latest

script:
  - npm test
  - npm run build
  - npm install --prefix app
  - npm test --prefix app
  - npm run build --prefix app