import React from 'react';
import { Panel } from 'react-bootstrap';

class Hops extends React.Component {
    render() {
        return (
            <Panel header="Hops">
                Same table or maybe grid for Hops
            </Panel>
        )
    }
}

export default Hops;